spring.application.name=backend
server.port=${PORT:8088}

# ── MongoDB ───────────────────────────────────────────────────────────────────
# Set MONGO_URI env var on Render/Railway to override this Atlas connection string
spring.data.mongodb.uri=${MONGO_URI:mongodb+srv://EndUser:w6qug_jXAqda-J6@blink-exam.lsepeow.mongodb.net/examdb?retryWrites=true&w=majority&appName=Blink-Exam}

# ── JWT ───────────────────────────────────────────────────────────────────────
# Must be at least 32 characters. Override with JWT_SECRET env var in production.
jwt.secret=${JWT_SECRET:fecc056b8a2d4935b16677ffd4540a1bdcb23018b6e6f3a96d719631a3dc4a17}

# ── CORS ──────────────────────────────────────────────────────────────────────
# Set CORS_ALLOWED_ORIGIN to your Vercel frontend URL on Render/Railway
cors.allowed-origin=${CORS_ALLOWED_ORIGIN:https://online-examination-system-ivory.vercel.app}
